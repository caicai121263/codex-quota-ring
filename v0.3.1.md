# Codex Quota Ring v0.3.1 更新清单

> 状态：已实现，待 Windows 实机验收  
> 版本主题：边缘停靠与自动收起  
> 目标平台：Windows 10/11 x64  
> 基线版本：v0.3.0

## 实施记录（2026-07-29）

- 已完成左、右、上边缘的吸附判定、松手检测和 32px 向内拖动解除。
- 已完成 180ms 窗口动画、程序移动忽略期和动画误吸附保护。
- 已完成 `108 × 36` 上边缘迷你条、`42 × 112` 侧边迷你条、微型额度环及 `5h` / `周` 标签。
- 已完成 150ms 悬停展开、800ms 离开收回，设置打开时不会自动收回。
- 已完成设置与托盘开关、v0.3.0 配置迁移、多显示器偏移和 Windows 工作区计算。
- 未增加全局鼠标钩子；仅在窗口最近发生移动时查询鼠标左键是否已释放。
- 前端测试 10 项通过，Rust 与模拟 App Server 测试 20 项通过。
- TypeScript、Vite、Tauri release 和 Windows x64 NSIS 打包通过。
- 已生成产品版本和文件版本均为 `0.3.1` 的安装包。

下方 Windows 10/11、多 DPI、任务栏边缘和双显示器项目仍需在安装后手工验收。

## 版本目标

为悬浮额度轨道增加可选的贴边停靠模式。用户将完整面板拖到屏幕左、右或上边缘并松手后，窗口自动吸附并收缩为迷你额度条；鼠标悬停时滑出完整面板，离开后自动收回。

## 已确认的交互参数

- [x] 支持左、右、上边缘，不支持底部，避免与任务栏冲突。
- [x] 距离工作区边缘 12 个逻辑像素内触发候选吸附。
- [x] 最后一次窗口移动后 250 毫秒视为拖动结束。
- [x] 迷你条悬停 150 毫秒后展开完整面板。
- [x] 鼠标离开完整面板 800 毫秒后自动收回。
- [x] 动画时长约 180 毫秒。
- [x] 向屏幕内部拖动完整面板超过 32 个逻辑像素时解除吸附。
- [x] 功能默认关闭，由设置或托盘主动启用。

## 迷你额度条

- [ ] 上边缘尺寸约为 `108 × 36` 逻辑像素。
- [ ] 左右边缘尺寸约为 `42 × 112` 逻辑像素。
- [ ] 显示微型环状图、主额度百分比以及 `5h` / `周` 标识。
- [ ] 迷你模式不显示倒计时、Credits、设置、刷新和诊断信息。
- [ ] 继续遵循健康、注意、危险和不可用颜色。
- [ ] `stale` 状态保留额度颜色并降低透明度。

## 停靠状态机

- [ ] 仅在启用贴边收起、设置关闭且窗口位置未锁定时检测拖动吸附。
- [ ] 程序主动调整窗口时暂停吸附检测，避免动画或设置展开造成误判。
- [ ] 已停靠窗口悬停展开后，可以沿屏幕内部方向拖动解除吸附。
- [ ] 沿当前边缘拖动只更新停靠位置，不解除吸附。
- [ ] 打开设置时保持完整展开，关闭设置后恢复自动收回逻辑。
- [ ] 刷新、点击和表单交互期间不意外收回。

## 多显示器与高 DPI

- [ ] 使用 Windows 可用工作区而非整块屏幕，避开任务栏。
- [ ] 吸附阈值、解除距离和迷你尺寸均按逻辑像素换算。
- [ ] 记录停靠显示器、边缘和沿边逻辑偏移。
- [ ] 在不同 DPI 显示器之间重新计算物理坐标。
- [ ] 显示器拔除后迁移至主显示器可见边缘。
- [ ] 停靠位置始终限制在对应工作区范围内。

## 配置与托盘

新增持久化配置：

```ts
type EdgeDockPreferences = {
  edgeDockEnabled: boolean;
  dockEdge: 'left' | 'right' | 'top' | null;
  dockMonitorId: string | null;
  dockOffset: number | null;
  dockAutoCollapseDelayMs: number;
};
```

- [ ] v0.3.0 配置自动迁移并采用安全默认值。
- [ ] 设置增加“启用贴边收起”开关。
- [ ] 托盘增加“贴边收起”开关并与设置同步。
- [ ] 关闭功能时恢复完整悬浮轨道。
- [ ] “锁定窗口位置”状态下仍允许自动展开/收回，但不能拖动解除吸附。

## 测试清单

### Rust

- [ ] 左、右、上边缘阈值判定。
- [ ] 最近边缘选择和底部边缘排除。
- [ ] 沿边偏移限制。
- [ ] 32px 向内拖动解除吸附。
- [ ] v0.3.0 配置迁移。
- [ ] 动画期间不触发吸附检测。

### 前端

- [ ] 三种边缘的迷你标签和布局。
- [ ] 迷你环颜色及 `stale` 样式。
- [ ] 150ms 展开和 800ms 收回调度。
- [ ] 设置打开时取消自动收回。
- [ ] 功能关闭后不进入迷你模式。

### Windows 手工验收

- [ ] Windows 10 / 11。
- [ ] 100%、125%、150%、200% DPI。
- [ ] 左、右、上边缘吸附。
- [ ] 任务栏位于不同边缘。
- [ ] 不同 DPI 双显示器之间拖动。
- [ ] 停靠显示器断开后的恢复。
- [ ] 设置、托盘、锁定位置和自动显隐联动。

## 隐私边界

本次更新仅增加本地窗口几何和鼠标进入/离开交互，不新增账户接口、全局鼠标钩子、键盘钩子、遥测或额度历史。
