# Codex Quota Ring v0.3.0 预更新功能清单

> 状态：已实现，待 Windows 实机验收  
> 版本主题：可靠性与智能驻留  
> 目标平台：Windows 10/11 x64  
> 基线版本：v0.2.0

## 实施记录（2026-07-29）

v0.3.0 已按本清单完成代码实现和自动化验证，主要交付包括：

- 单实例、单飞刷新、5 秒成功缓存、候选 App Server 回退和无窗口子进程。
- `ready` / `stale` / `loading` / `unavailable` 状态，以及刷新失败时保留最近有效快照。
- 只读账户状态、Credits / Reset Credits / 套餐字段的容错解析。
- Codex / ChatGPT 进程启停联动、自身 App Server 子进程排除和手动隐藏抑制。
- 设置面板按 Windows 可用工作区向上或向下展开，多显示器离屏恢复、几何保存防抖和位置锁定。
- 托盘设置、主环切换、位置锁定、置顶、开机启动，以及开机启动后隐藏。
- 实时倒计时、重置到期刷新、睡眠/长计时跳跃恢复和显示时刷新。
- 脱敏诊断复制、v0.2.0 配置迁移和损坏配置备份。

自动化结果：

- 前端单元测试：9 项通过。
- Rust 单元与模拟 App Server 集成测试：17 项通过。
- TypeScript、Vite 和 Tauri release 构建通过。
- 已生成 Windows x64 NSIS 安装包，产品版本和文件版本均为 `0.3.0`。

协议核对说明：

- `account/usage/read` 的官方响应是令牌活动摘要与日桶，不包含 Credits 余额。本版本没有为了推测 Credits 而调用该接口；Credits 只在 `account/rateLimits/read` 或 `account/read` 实际返回对应字段时显示，缺失时隐藏。
- 本版本未接入任何账户写接口，包括登录、退出、购买、消费 Reset Credit 或额度重置。

仍需在目标机器完成本清单末尾的 Windows 10/11、多 DPI、双显示器、睡眠唤醒和覆盖安装手工验收。

## 版本目标

v0.3.0 不扩展为复杂的额度统计平台，继续保持轻量、本地、只读的桌面额度工具定位。本次更新优先解决长期运行中最重要的问题：

- 避免重复刷新和多个 App Server 子进程。
- 读取失败时保留最近一次有效额度。
- 避免重复启动多个应用实例。
- 改善多显示器、高 DPI 和屏幕边缘下的窗口行为。
- 根据 Codex / ChatGPT 运行状态智能显示或隐藏。
- 增加 Credits 等只读账户额度信息。
- 提升错误状态、托盘控制和启动体验。

## 一、刷新协调与 App Server 稳定性

### 1.1 单飞刷新

- [ ] 启动刷新、定时刷新、托盘刷新和界面刷新统一进入一个后端刷新协调器。
- [ ] 同一时刻只允许一个 `codex.exe app-server --stdio` 请求运行。
- [ ] 已有刷新正在进行时，后续刷新复用同一结果，不重复启动子进程。
- [ ] 前端刷新按钮在请求期间显示忙碌状态并禁止重复点击。
- [ ] 所有刷新入口继续使用 Windows `CREATE_NO_WINDOW`，不得出现命令行弹窗。

### 1.2 短时结果缓存

- [ ] 最近 5 秒内已经成功读取额度时，重复请求直接返回缓存。
- [ ] 手动“强制刷新”允许跳过短时缓存，但仍必须遵守单飞机制。
- [ ] 缓存仅存在内存中，不新增额度历史文件。

### 1.3 App Server 异常恢复

- [ ] 子进程提前退出、响应超时或返回异常 JSON 时能够正常清理进程。
- [ ] 下次刷新重新发现 `codex.exe` 并建立新连接。
- [ ] 候选路径启动失败时继续尝试下一个候选路径。
- [ ] 错误信息只能显示脱敏后的路径，不显示用户目录全路径。
- [ ] 应用退出时确保自身启动的 App Server 子进程被终止。

## 二、保留最近有效额度

### 2.1 新增过期状态

- [ ] 额度状态增加 `stale`，与 `ready`、`loading`、`unavailable` 区分。
- [ ] 曾经成功读取额度后，如果后续刷新失败，继续显示上次有效数据。
- [ ] 过期数据使用降低透明度或灰色状态标记，但保留原额度颜色。
- [ ] 显示“数据可能已过期”和最后成功更新时间。
- [ ] 用户可查看本次刷新失败原因。

### 2.2 首次启动失败

- [ ] 从未成功读取过额度时才显示完整的不可用占位。
- [ ] 明确区分以下状态：
  - 未找到 Codex。
  - Codex 尚未登录。
  - App Server 启动失败。
  - 初始化失败。
  - 额度请求超时。
  - 返回协议无法识别。
  - 没有返回 5 小时或周额度。

## 三、应用单实例

- [ ] 接入 Tauri 单实例能力。
- [ ] 第二次启动程序时不创建新的托盘图标和刷新任务。
- [ ] 第二次启动时显示已有窗口，并根据用户操作允许聚焦。
- [ ] 如果已有窗口在屏幕外，显示前先恢复到当前显示器工作区。
- [ ] 单实例消息中不得携带账号、令牌或项目内容。

## 四、Credits 只读显示

### 4.1 数据读取

- [ ] 初始化后调用只读的 `account/read` 判断登录与套餐状态。
- [ ] 继续使用 `account/rateLimits/read` 读取额度窗口。
- [ ] 可选调用只读的 `account/usage/read` 补充 Credits 数据。
- [ ] 解析以下可用字段：
  - Credits 余额。
  - Credits 上限。
  - Reset Credits 可用次数。
  - 套餐类型。
- [ ] 任一字段缺失时允许单独为空，不影响 5 小时和周额度显示。

### 4.2 界面显示

- [ ] 设置增加“显示 Credits”开关，默认开启。
- [ ] Credits 有数据时显示在右侧紧凑区域或底部辅助信息中。
- [ ] Credits 无数据时隐藏对应区域，不显示误导性的 `0`。
- [ ] 不提供购买、消费、重置额度或任何账户写操作。

## 五、Codex / ChatGPT 启停联动

### 5.1 进程检测

- [ ] 设置增加“Codex 启动时自动显示”，默认关闭。
- [ ] 设置增加“Codex 关闭后自动隐藏”，默认关闭。
- [ ] 仅检测 `ChatGPT.exe` 和 `codex.exe` 的进程名及父进程关系。
- [ ] 排除 Codex Quota Ring 自己启动的 `codex.exe` 子进程。
- [ ] 不读取完整命令行、可执行文件内容、会话文件或项目目录。

### 5.2 显示规则

- [ ] 自动显示窗口时不抢夺当前应用焦点。
- [ ] 自动显示后立即刷新额度。
- [ ] 连续两次确认目标进程关闭后才执行自动隐藏，避免短暂进程切换造成闪烁。
- [ ] 用户在当前 Codex 运行周期内手动隐藏后，本周期不再自动弹出。
- [ ] 下一次真实的 Codex / ChatGPT 启动周期重新允许自动显示。
- [ ] 窗口隐藏时继续遵守“不发起额度读取”的现有规则。

## 六、窗口与多显示器可靠性

### 6.1 设置面板几何恢复

- [ ] 打开设置前记录窗口的完整位置和尺寸。
- [ ] 设置面板优先向下展开；靠近屏幕底部时改为向上展开。
- [ ] 关闭设置后恢复打开前的窗口位置和内部尺寸。
- [ ] 调整界面缩放时保持窗口锚点稳定，避免跳到其他显示器。

### 6.2 多显示器与 DPI

- [ ] 所有窗口计算统一区分逻辑像素和物理像素。
- [ ] 支持不同缩放比例的多显示器之间拖动。
- [ ] 显示器拔除、分辨率变化或 DPI 变化后检查窗口是否仍与任一工作区相交。
- [ ] 窗口完全离屏时移动到主显示器可见区域。
- [ ] 保存位置前进行短时防抖，避免拖动时频繁写配置。

### 6.3 位置锁定

- [ ] 设置和托盘增加“锁定窗口位置”开关。
- [ ] 锁定后禁用窗口拖动和右下角拉伸。
- [ ] 解锁后恢复移动与缩放功能。

## 七、实时倒计时与系统恢复

- [ ] 前端每分钟仅根据本地 `resetsAt` 更新时间文本，不触发 App Server 请求。
- [ ] 额度已经到达重置时间时，在窗口可见的前提下请求一次刷新。
- [ ] 检测系统睡眠、休眠或长时间锁屏造成的计时跳跃。
- [ ] 系统恢复后，如果窗口可见，立即刷新一次额度。
- [ ] 窗口从隐藏变为显示时立即刷新。
- [ ] 多个恢复条件同时发生时仍由单飞刷新协调器去重。

## 八、托盘与启动体验

- [ ] 托盘菜单增加“设置”入口。
- [ ] 托盘菜单增加“主环显示：5 小时 / 周额度”切换。
- [ ] 托盘菜单增加“锁定窗口位置”开关。
- [ ] 托盘菜单增加“始终置顶”开关。
- [ ] 托盘菜单增加“开机启动”开关。
- [ ] 增加“开机启动后隐藏”设置，默认关闭。
- [ ] 手动启动程序时忽略“启动后隐藏”，正常显示窗口。
- [ ] 托盘状态与设置面板修改后保持同步。

## 九、诊断与隐私

### 9.1 可理解的错误

- [ ] 设置面板增加“诊断信息”区域。
- [ ] 提供“复制诊断信息”按钮。
- [ ] 诊断内容仅包含：
  - 应用版本。
  - Windows 版本。
  - 是否找到 Codex。
  - 使用的候选来源类型。
  - 最后成功刷新时间。
  - 最近一次脱敏错误类型。
- [ ] 不复制 App Server 原始 JSON。

### 9.2 隐私边界

- [ ] 不读取或保存 Cookie、Token、`auth.json`。
- [ ] 不读取 Codex 对话、提示词、会话文件或项目内容。
- [ ] 不添加遥测、分析或崩溃上报 SDK。
- [ ] 不保存额度历史和任务消费历史。
- [ ] 不提供 API Key 输入。
- [ ] 不提供账户登录、退出、额度重置或其他写操作。

## 配置结构调整

v0.3.0 计划新增以下持久化偏好：

```ts
type PreferencesV030 = {
  showCredits: boolean;
  autoShowOnCodex: boolean;
  autoHideOnCodexClose: boolean;
  startHiddenOnAutostart: boolean;
  lockWindowPosition: boolean;
};
```

- [ ] 所有新增字段提供默认值。
- [ ] v0.2.0 配置缺少新字段时自动迁移。
- [ ] 配置损坏时备份损坏文件并恢复安全默认值。
- [ ] 升级不得丢失刷新间隔、主环类型、界面缩放、位置、置顶和开机启动设置。

## 接口调整

规范化额度快照计划扩展为：

```ts
type QuotaStatusV030 = {
  state: 'ready' | 'stale' | 'loading' | 'unavailable';
  message: string | null;
  fiveHour: LimitWindow | null;
  weekly: LimitWindow | null;
  credits: {
    balance: number | null;
    limit: number | null;
    resetCreditsAvailable: number | null;
    planType: string | null;
  } | null;
  updatedAt: number | null;
  lastSuccessAt: number | null;
  errorCode: string | null;
};
```

- [ ] 前端仍只接收规范化数据，不接触 App Server 原始响应。
- [ ] 错误码使用应用内部枚举，不直接展示底层敏感错误。
- [ ] Credits 数据与额度窗口独立解析。

## 测试清单

### Rust 单元测试

- [ ] 多个刷新请求只启动一个底层读取。
- [ ] 5 秒缓存命中与强制刷新。
- [ ] 刷新失败保留最近有效快照。
- [ ] 首次失败返回不可用状态。
- [ ] `account/read` 未登录与已登录响应。
- [ ] Credits 字段缺失、类型变化和异常值。
- [ ] Codex / ChatGPT 进程分类及自身子进程排除。
- [ ] v0.2.0 配置迁移。
- [ ] 脱敏诊断信息不包含用户目录。

### 前端单元测试

- [ ] `ready`、`stale`、`loading`、`unavailable` 显示。
- [ ] Credits 缺失时不显示错误的零余额。
- [ ] 实时倒计时跨越重置时刻。
- [ ] 锁定位置时禁用拖动和拉伸。
- [ ] 设置向上/向下展开的几何计算。
- [ ] 离屏窗口恢复到有效工作区。

### 集成测试

- [ ] 模拟 JSON-RPC 初始化、账户读取、额度读取与 Credits 读取。
- [ ] App Server 超时、异常响应和提前退出后的恢复。
- [ ] 启动刷新与手动刷新并发。
- [ ] 第二个应用实例唤醒已有窗口。
- [ ] 进程启停联动和手动隐藏抑制。

### Windows 手工验收

- [ ] Windows 10 和 Windows 11 x64。
- [ ] 100%、125%、150%、200% DPI。
- [ ] 单显示器和不同 DPI 的双显示器。
- [ ] 窗口位于屏幕上、下、左、右边缘时打开设置。
- [ ] 显示器断开后窗口仍可见。
- [ ] 连续快速点击刷新不出现多个命令行窗口。
- [ ] Codex 自动显示不抢焦点。
- [ ] 手动隐藏后不重复自动弹出。
- [ ] 睡眠唤醒后额度恢复刷新。
- [ ] 从 v0.2.0 覆盖安装后原设置保留。

## 明确不进入 v0.3.0

以下功能延后评估，避免本次更新范围失控：

- 任务栏条或超紧凑条形模式。
- 单次任务消费估算。
- 额度历史、消费历史和 CSV 导出。
- Windows 通知与额度阈值提醒。
- 应用内自动更新。
- GitHub / Gitee 双更新源。
- 中英文界面切换。
- Windows UI Automation 读取 Codex 页面。
- OpenAI API Key 或任何远程额度接口。

## 推荐实施顺序

1. 单实例、刷新协调器和最近有效快照。
2. App Server 账户状态与 Credits 规范化。
3. 多显示器窗口几何和设置面板恢复。
4. Codex / ChatGPT 进程联动。
5. 实时倒计时、睡眠恢复和显示恢复刷新。
6. 托盘菜单、位置锁定和启动后隐藏。
7. 脱敏诊断、配置迁移和完整测试。
8. 版本升级至 v0.3.0，生成 Windows x64 NSIS 安装包。

## 发布门槛

- [ ] 前端与 Rust 自动测试全部通过。
- [ ] 安全审计确认没有新增敏感数据读取。
- [ ] 隐藏状态不启动新的额度读取。
- [ ] 不存在重复实例和重复 App Server 进程。
- [ ] 刷新失败不会覆盖最近有效数据。
- [ ] 多显示器和高 DPI 验收通过。
- [ ] v0.2.0 配置迁移和覆盖安装通过。
- [ ] NSIS 安装包版本、图标和卸载流程正确。
